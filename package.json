{
  "name": "listerine",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test:js": "node --test --test-reporter=spec && node -e \"console.log('Running JavaScript coverage gate...')\" && node ./node_modules/c8/bin/c8.js --all --include app/web/static/*.js --branches 100 --functions 100 --lines 100 node --test && node -e \"console.log('JavaScript coverage gate passed.')\""
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "jsdom": "^29.0.0",
    "playwright": "^1.52.0"
  }
}
